# `:thomas:ontology`

Platform-independent, behavior-free CT-V2-02 vocabulary and governed source bindings.

This module defines concepts Thomas may later reason with. It does not observe users, select actions, evaluate eligibility, classify safety, invoke a model, render language, persist psychological data, or authorize runtime use. Every catalog concept is either a definition or an unopened candidate; every source binding is provenance only.
