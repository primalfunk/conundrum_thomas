# `:thomas:domain`

Platform-independent shared contracts and future canonical state types. This module may not depend on Android, persistence, JNI, speech, a concrete model, engine, safety, or runtime. CT-V2-00 contains only the narrow renderer request contract.
