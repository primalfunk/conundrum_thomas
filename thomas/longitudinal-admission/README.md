# Longitudinal admission

Pure deterministic CT-V2-07 admission and revision policy. This module defines structured synthetic-only requests, lifecycle and privacy semantics, canonical fingerprints, and mutation plans. It has no database, Android, network, model, prompt, renderer, or therapeutic-policy dependency and cannot persist anything.
