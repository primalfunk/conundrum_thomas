# `:thomas:longitudinal`

Platform-independent personal-evidence and temporal domain foundation.

The module defines sources, assertions, life entities, personal-evidence provenance, temporal uncertainty, contradictions, corrections, supersession, hypothesis dependencies, uncertain entity identity, and coverage status. It has no Android, Room, SQLite, network, model, therapeutic-policy, or production-persistence dependency.

It defines immutable structures and deterministic validation only. It contains no writer, admission engine, extractor, retrieval engine, profile mutator, or mode behavior.
