# Therapy longitudinal integration

This pure Kotlin module composes already-governed authorities for synthetic CT-V2-12 qualification. It captures the current user turn through a narrow admission port, evaluates current safety, fixes the CT-V2-05 route, requests a pre-turn-revision CT-V2-11 packet, applies a conservative memory-use gate, and returns typed plan and render-support data.

It contains no store implementation, SQL/JDBC, Android, model, prompt, renderer implementation, Journal policy, Biographer target logic, new therapeutic route, or production composition root. Ordinary surfacing is limited to one directly related historical object; zero is normal.
