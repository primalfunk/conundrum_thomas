# `:thomas:engine`

Future formulation, policy, and intervention selection implementations. It accepts structured state and has no platform or concrete-model dependency. CT-V2-00 defines only a typed policy boundary and no therapeutic logic.
