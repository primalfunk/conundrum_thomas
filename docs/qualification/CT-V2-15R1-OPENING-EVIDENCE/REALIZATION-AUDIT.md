# Bounded realization-feasibility audit

The affected branch is the no-memory ordinary Therapy realization catalog: 23 deliverable CoreOrdinaryActions IDs / 22 templates (the two verification IDs share one). Silent pause/wait return before realization. The branch with surfaced historical memory supplies listOf(complete) and changes to historical semantic acts; it is not part of therapyVariants. Other mode adapters and the legacy non-core fallback remain unchanged. No universal renderability claim is made for those other semantic families.

Before repair, 20 ordinary templates had one distinct form/opening; established-content reflection and further-expression invitation had three. All use exact-response window 6 and opening window 4, with rejection at two equal openings. Progression prevents same-action loops only at an unchanged evidence revision; changed evidence and explicitly authorized repeat can allow the same act while prior realizations remain recent. The finite renderer guarantee cannot rely on a nonexistent cross-revision exclusion. The remedy gives every ordinary template seven distinct openings. The machine-readable catalog records the exact authorized alternatives; original base wording is preserved.

For seven candidates with pairwise different openings, each recurrence-blocked candidate has a disjoint nonempty witness set in the last six responses. An exact duplicate consumes a matching response; a repeated opening consumes two entries in the last four. No response can witness two candidates because their openings differ. At most six candidates can be blocked, so at least one survives. The finite test enumerates all 2,269 exact/opening blocking equivalence classes fitting six responses, including both simultaneously blocked openings, mixed exact matches, and successively rejected candidates. A separate 30-response sequence proves window rollover, with ten identical renders per state. Each ordinary catalog is also tested with every six-of-seven exact blocking subset. These are renderer guarantees conditional on the unchanged full semantic and output-budget constraints, not new therapeutic eligibility rules.

The two verification IDs retain catalog order explicitly. Their preferred form is still I might be understanding this as [authorized understanding]. Is that right? The realizer submits it to the unchanged validator; the existing exhaustive fallback then tests catalog forms in order. The old preferred candidate can be validated twice (initial attempt and deduplicated fallback list); both rejections remain visible and deterministic. No new search loop or validator change was needed. Other ordinary actions retain seeded reference choice and unused-opening preference, followed by complete catalog-order fallback.

Semantic review source: CoreOrdinaryActions.renderSpecification. Verification alternatives retain the exact tentative support, explicit uncertainty and one question checking correctness. Summaries contain only the corresponding established/confirmed support. Missing-piece questions request only that piece. Influence questions concern agency, not blame. Option questions ask for the user's own options or choice without recommendations. Plan questions retain the chosen option, one small first step and timing. Outcome questions do not infer success/failure. Close and direction forms preserve user choice; invitation forms add no topic. Every generated candidate retains the same governed semantic-unit manifest and receives the full validator. The new dynamic forms fit the prior maximum character boundary; budgets were not increased.

Production scope beyond the inherited identity repair: TherapyRenderCommandAdapter.kt (finite catalog), RendererContracts.kt (explicit optional catalog-order metadata), DeterministicReferenceRealizer.kt (honor that metadata). DeterministicRenderValidator.kt, GovernedLanguageRenderer.kt, procedural engine, runtime delivery history, identity allocator, dispatch, UI modes, model/speech/Pattern Engine and corpus are unchanged from the incoming candidate.
