# CONUNDRUM THOMAS V2

## CT-V2-15 - ANDROID PRODUCTION INTEGRATION

### Development Work Order

**Status:** AUTHORIZED FOR IMPLEMENTATION

**Predecessor:** CT-V2-14 - Personal Data Governance & Persistence Qualification

**Canonical repository:** `C:\Android Studio Projects\ConundrumThomasV2`

**Canonical branch:** `main`

Accepted entry: HEAD `d8f4b5cb03a9a3a282ea8e8be438376a74a117b9`,
tree `a3bd9fb0ca4ef25a1ed3716ef97cc1773fbe34d4`, annotated
tag `ct-v2-14-personal-data-governance-persistence` (object
`a5a70d722296d6340202b8e18006a9ae43aa4184`), clean worktree,
seven commits ahead of `origin/main`, canonical forward-plan SHA-256
`bfcd281f95e8aa188cc585121e740a635d1899a1f7661bbe69e798d990705886`,
root `fdp.txt` absent, and V1 register 24/24 denied.

## 1. Purpose and invariant

Compose the qualified Journal, Biographer, Therapy, longitudinal admission,
protected persistence, retrieval, longitudinal Therapy, rendering, lifecycle,
and authorized speech surfaces into one production-shaped Android runtime.

> THOMAS DECIDES. THE RENDERER REALIZES. A LATER MODEL HAS NO THERAPEUTIC AUTHORITY.

The authoritative path is committed user input -> governed mode/input boundary
-> admission where applicable -> longitudinal state/retrieval -> mode policy ->
semantic action -> governed render command -> validated rendering -> final
assistant artifact -> optional final-artifact TTS. Direct user-to-model,
database-to-renderer, UI-to-technique, and model-output-to-evidence paths are
prohibited.

## 2. Entry and repository conditions

Verify root, canonical `.git`, HEAD/tree/branch/worktree/remote/ahead state,
predecessor tag, canonical-root verifier, `git diff --check`, temporary metadata
count, forward-plan path/size/hash/copy count, root `fdp.txt`, V1 deny-by-default,
composition roots, assistant-text paths, speech implementations, persistence,
and absence of real personal fixtures. Do not reset valid intervening work,
relocate Git metadata, duplicate the plan, push, or use real-user qualification
data.

## 3. Canonical production composition

Provide exactly one auditable Android V2 composition root joining all three
modes, governed admission and persistence, retrieval, Therapy integration,
render-command construction, CT-V2-13 validation, lifecycle, and custody.
Avoid hidden service locators and parallel legacy controllers. The session
orchestrator may sequence but may not decide safety, route, technique, Journal
posture, Biographer target, relevance, evidence status, or interpretation.

The CT-V2-13 deterministic reference realizer remains the production realizer.
Model-backed realizers, GGUFs, remote inference, LM Studio, and cloud language
generation remain zero.

## 4. Persistence and physical-device gate

Compose the CT-V2-14 protected store through governed domain operations only:
AndroidKeyStore, `noBackupFilesDir`, atomic persistence, revision lineage,
privacy, correction, deletion, rebuild, protected backup, export, restore,
reset, migration, and fail-closed corruption. UI and runtime receive no generic
DAO, SQL, raw key, or mutable store bypass.

On a physical Android target, preferably TCL 9491G, prove synthetic create,
persist, process termination, reopen, reboot, identical recovery, protected
backup, restore, reset, post-reset absence, key-loss failure, storage location,
backup exclusion, and device-reported KeyStore properties. An emulator cannot
substitute for this mandatory gate.

## 5. Mode integration

- Journal preserves commit-before-evidence, drafts as non-evidence, revision and
  privacy semantics, and `NO_RESPONSE` as zero assistant artifact, renderer
  call, and TTS call. `REFLECT` and `ASK_ONE_QUESTION` remain bounded.
- Biographer displays only its governed target, admits user answers through the
  normal pipeline, preserves coverage, and never turns prompts/unanswered
  questions into evidence.
- Therapy runs current safety, CT-V2-05 route, bounded CT-V2-11 retrieval,
  CT-V2-12 memory support, governed render command, and validated output in that
  order. UI, renderer, and controller cannot choose technique.
- Safety may supersede ordinary Therapy only through CT-V2-04. Historical risk
  or instruction-like text has no current safety authority.

Prove Journal -> Therapy, Biographer -> Therapy, correction, deletion, and
privacy end to end with provenance, uncertainty, bounded renderer visibility,
and no stale derived influence.

## 6. Android UI, input, and lifecycle

Provide a clear three-mode Compose interface with visible transcript, typed
draft, explicit send, existing governed posture controls, processing/listening/
speaking state, and personal-data custody actions. UI is a projection and
command surface, not policy. Mode switching must not commit drafts, duplicate
evidence, leak privacy, carry stale targets/postures, or reinterpret drafts.

Typed input is canonical. Qualify blank/long/repeated send, processing races,
mode change, lifecycle recreation, committed versus draft state, and retry.
Serialize turns and use explicit identity so obsolete results cannot answer a
later turn.

STT, if admitted, is a transcript adapter with distinct finish/use and
cancel/discard semantics and disclosed network/privacy behavior; otherwise it
must remain disabled. TTS, if admitted, consumes only a final visible validated
artifact and never candidates, packets, prompts, hidden data, or silence.

## 7. Custody, failure, and privacy

Expose explicit human/machine export, protected backup and separate recovery
key, integrity-checked empty-target restore, and unmistakable complete reset.
The UI never receives raw key material, and reset cannot claim to erase
independently held exports/backups.

Typed failures cover unavailable persistence/KeyStore, corruption, schema,
retrieval, policy, renderer and fallback, speech, and lifecycle interruption.
Failures may safely degrade but never transfer psychological authority. Sanitize
logs and exclude user content from analytics, exceptions, filenames,
notifications, reports, and dumps.

## 8. Qualification and audits

Run JVM regression, meaningful Android instrumentation, physical-device tests,
canonical clean build/lint/provenance, Git verifier/fsck/diff check, V1 audit,
APK/tree artifact scans, backup-rule inspection, authority audit, and
reproducibility observation. Record task/test/lint counts, APK sizes/hashes,
device identity, performance medians/worst values, and environment-impossible
checks honestly.

Required authority posture: one production application root and one protected
store root; zero legacy/V1 conversation roots; zero UI direct persistence or
route paths; zero renderer/realizer/model persistence paths; zero renderer
route/safety/target paths; zero assistant-response-to-user-evidence paths; zero
model artifacts/realizers/remote inference; zero generic Android backup; zero
uncontrolled plaintext personal stores.

Inspect debug and unsigned release APKs and Git for real data, test databases,
backups/exports, keys, models, prompts, packets, transcripts, V1 therapeutic
assets, build outputs, credentials, personal email addresses, and local paths.

## 9. Scope, completion, and next phase

No model admission, cloud system, account, remote storage, new Therapy/safety
policy, diagnosis, V1 migration, production signing/release, telemetry, passive
sensing, or app-store/legal work is authorized. Use coherent commits. Do not
push.

Only after every mandatory gate passes, create annotated tag
`ct-v2-15-android-production-integration` at the final clean qualification
commit. Physical-device failure blocks COMPLETE; optional speech failure may be
accepted only by safely disabling speech.

The successful disposition is
`CT_V2_15_ANDROID_PRODUCTION_INTEGRATION_COMPLETE` with a single canonical V2
runtime, protected Android persistence, three governed modes, longitudinal
cross-mode behavior, safety, rendering, silence, lifecycle, serialization,
custody, typed input, zero model/UI/renderer therapeutic authority, V1 denial,
and preserved CT-V2-04 through CT-V2-14 boundaries.

The completion report must provide repository, production architecture,
Android application, persistence, Journal, Biographer, Therapy, safety, speech,
target-device, authority-audit, qualification, limitations, and recommendation
evidence. Do not open a next phase without Principal authorization.
